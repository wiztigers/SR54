sudo: required

before_install:
  - mkdir -p output
  - echo $TRAVIS_BUILD_DIR
  - pwd
  - ls -l
  - gem install asciidoctor
  - echo $GEM_HOME
  - gem env

script:
  - asciidoctor core.adoc

after_success:
  - echo done!

